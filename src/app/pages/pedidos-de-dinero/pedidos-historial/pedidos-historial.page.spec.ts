import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PedidosHistorialPage } from './pedidos-historial.page';
/*describe('PedidosHistorialPage', () => {
  let component: PedidosHistorialPage;
  let fixture: ComponentFixture<PedidosHistorialPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(PedidosHistorialPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
