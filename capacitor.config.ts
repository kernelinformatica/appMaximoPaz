import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
   appId: 'com.coopaz',

  appName: 'Coopaz',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
